package com.tjoeun.syncronizedMethodTest;

public class LibraryMain {

	public static void main(String[] args) {
		/*
		Library library = new Library();
		System.out.println(library.bookList);
		
//		ArrayList의 remove(object) 메소드를 실행하면 삭제할 객체가 있으면 true, 없으면 false를
//		리턴하고 remove(index) 메소드를 실행하면 삭제할 index의 데이터를 리턴하고 삭제한다.
		String book = library.bookList.remove(0);
		System.out.println(book);
		System.out.println(library.bookList);
		*/
		
		
	}
	
}
